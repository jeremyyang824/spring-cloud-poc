spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
      #discovery:
      # enabled: true
      # serviceId: configserver

      #uri: ${CONFIG_SERVER_URL:http://localhost:8888}  #none-eureka-config
